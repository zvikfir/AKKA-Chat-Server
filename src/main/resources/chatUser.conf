akka {
    actor {
        provider = remote
    }
    remote {
         artery {
              enabled = on
              transport = tcp # See Selecting a transport below
              canonical.hostname = "127.0.0.1"
              canonical.port = 0
         }
    }
}
